kind: Service                             
apiVersion: v1
metadata:
  name: sample_app
  labels:
    app: sample_app
spec:
  ports:
    - port: 8080                               
      targetPort: 80                    
      nodePort: 31200
  selector:
    app: sample_app                    
  type: LoadBalancer                   
